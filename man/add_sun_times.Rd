% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hobo_do.R
\name{add_sun_times}
\alias{add_sun_times}
\title{add_sun_times}
\usage{
add_sun_times(df, lat, lon, time_col = "date_time", tz = "Asia/Taipei")
}
\arguments{
\item{df}{A data frame containing a POSIXct time column.}

\item{lat, lon}{Site latitude and longitude in decimal degrees (length 1).}

\item{time_col}{Name of the POSIXct time column. Default \code{"date_time"}.}

\item{tz}{Time zone for the returned sun times. Default \code{"Asia/Taipei"}.}
}
\value{
\code{df} with two added POSIXct columns, \code{sunrise} and
  \code{sunset}, matched to each row's calendar date.
}
\description{
Attach per-day sunrise and sunset times to a data frame of
  readings, computed from a site's coordinates with the \pkg{suncalc}
  package. Replaces the manual lookup of sunrise/sunset for each deployment
  date; the resulting \code{sunrise}/\code{sunset} columns are consumed by
  \code{\link{calculate_do}}.
}
\examples{
\dontrun{
df <- add_sun_times(df, lat = 22.3900, lon = 120.5777)
}
}
