% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gantt.R
\name{gantt_text}
\alias{gantt_text}
\title{Plain-text Gantt for terminal and Neovim}
\usage{
gantt_text(
  df,
  colors = NULL,
  system_labels = NULL,
  done_cols = NULL,
  na_color = "#BFBFBF",
  notes = NULL
)
}
\arguments{
\item{df}{Data frame with columns \code{category}, \code{system}, then one
column per campaign. Campaign column names become header labels.}

\item{colors}{Named character vector mapping system identifiers to hex fill
colors, e.g. \code{c(tilapia = "#D8AEFF", prawn = "#F7CAAC")}.}

\item{system_labels}{Named character vector of short display labels for the
system column (Col 2). Defaults to \code{names(colors)}.}

\item{done_cols}{Character vector of campaign column names that are
completed. Completed cells with values get system fill; others with values
get white fill (planned). If \code{NULL} (default), all values get fill.}

\item{na_color}{Hex fill for \code{NA} / empty cells. Default grey
(\code{"#BFBFBF"}) suits sampling schedules. Use \code{"white"} for
processing-progress tables where \code{NA} means "not yet processed".}

\item{notes}{Optional character string added as a small-font footer.}
}
\value{
Invisibly returns \code{df}.
}
\description{
Prints a Unicode box-drawing table to the console. Same data contract as
\code{\link{gantt_table}}. Run from the project root with:
\code{Rscript -e "source('R/gantt_config.R'); aelab::gantt_text(gantt_schedule)"}
}
