% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ghg_flux.R
\name{plot_ghg_flux}
\alias{plot_ghg_flux}
\title{plot_ghg_flux}
\usage{
plot_ghg_flux(
  data,
  regression_result,
  ghg,
  fit_type = "linear",
  t_zero = 0,
  flag_colors = c(ok = "#2ca25f", discard = "#fc8d59", zero = "#74c7ef", no_data =
    "#aaaaaa")
)
}
\arguments{
\item{data}{Analyzer data processed by \code{tidy_ghg_analyzer()} and \code{convert_time()}.}

\item{regression_result}{Output tibble from \code{calculate_regression()}.}

\item{ghg}{Column name in \code{data} for the gas concentration (e.g. \code{"CH4"}, \code{"N2O"}).}

\item{fit_type}{Model used to draw the fitted line. Should match the \code{fit_type} used in
\code{calculate_regression()}. One of \code{"linear"} (default), \code{"exp_tz"},
\code{"exp_zhao18"}.}

\item{t_zero}{Reference time in seconds for \code{"exp_tz"} slope. Default 0.}

\item{flag_colors}{Named character vector mapping flag values to colors.}
}
\value{
A \code{ggplot} object with one facet per site. Each panel shows raw concentration
  points and the fitted line, with strip text showing slope, R², and flag.
}
\description{
Plot GHG concentration over time for each measurement window, overlaid with the
  fitted regression line and colored by quality flag. Use this for visual QC of
  \code{\link{calculate_regression}} output.
}
\examples{
data(n2o)
n2o_converted <- convert_time(n2o)
ref <- data.frame(
  date_time = n2o_converted$real_datetime[1],
  site = "S1", analyzer = "1074"
)
result <- calculate_regression(n2o_converted, ref, "N2O",
                               reference_time = "date_time", site = "site",
                               analyzer_code = "1074")
plot_ghg_flux(n2o_converted, result, "N2O")
}
